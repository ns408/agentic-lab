# --- Secrets and local-only files (never commit) ---
.env
*.env
!.env.example
.dev/
TEMP/
*.pem
*.key
*.p12
id_rsa
id_rsa.*
*secret*
*token*
*credentials*

# --- Claude Code local-only guardrails (machine-specific, private) ---
# Anything under .claude/local/ names private paths or carries leak-detection
# rules and must never be published or sent to CI.
.claude/settings.local.json
.claude/CLAUDE.local.md
.claude/local/

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- OS / editor ---
.DS_Store
.idea/
.vscode/
*.swp
